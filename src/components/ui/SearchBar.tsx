import { Search } from 'lucide-react';
import React from 'react'

const SearchBar = () => {
  return (
    <div>
        <Search className='w-6 h-6 hover:text-shop-accent-sky-blue hoverEffect'/>
        
    </div>
  );
};

export default SearchBar;